import { TestBed } from '@angular/core/testing';

import { SayingsNavRouteResolverService } from './sayings-nav-route-resolver.service';

describe('SayingsNavRouteResolverService', () => {
  beforeEach(() => TestBed.configureTestingModule({}));

  // it('should be created', () => {
  //   const service: SayingsNavRouteResolverService = TestBed.get(SayingsNavRouteResolverService);
  //   expect(service).toBeTruthy();
  // });
});
